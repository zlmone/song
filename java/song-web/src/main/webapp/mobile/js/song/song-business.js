$.extend(song, {    //获取加密过的手机号    getPhoneNumber: function () {        var phone = "",            dataphone = song.getConfig("phone");        if (window.phone) {            phone = window.phone.obtainPhone();// 加密过的手机号        } else if (dataphone && dataphone != "null") {            phone = dataphone;        } else {            phone = song.queryString("mobile") || song.queryString("phone");        }        return phone;    },    getPhoneParamter: function () {        return {phone: song.getPhoneNumber()}    },    getCHNumber: function (number) {        //获取数字的中文        var chs = ["零", "一", "二", "三", "四", "五", "六", "七", "八", "九", "十"];        var intValue = parseInt(number);        if (!isNaN(intValue) && intValue <= 3 && intValue >= 0) {            return chs[intValue];        }        return "";    }});