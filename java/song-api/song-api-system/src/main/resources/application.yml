# 默认的profile为dev，其他环境通过指定启动参数使用不同的profile，比如：#   启动参数最后添加 --spring.profiles.active=prod 默认参数可不添加启动#   开发环境：java -jar xxx.jar --spring.profiles.active=dev#   生产环境：java -jar xxx.jar --spring.profiles.active=prodspring:  profiles:    active: dev