package song.api.common.config;import song.api.common.base.IBaseDao;import tk.mybatis.spring.mapper.MapperScannerConfigurer;import java.util.Properties;/** * description: * author:          song * createDate:      2018/4/17 */public class MapperHelper {    public static  MapperScannerConfigurer getTKMapperConfigurer(String basePackage){        MapperScannerConfigurer mapperScannerConfigurer = new MapperScannerConfigurer();        mapperScannerConfigurer.setBasePackage(basePackage);        Properties properties = new Properties();//设置通用mapper的属性        // 这里要特别注意，不要把MyMapper放到 basePackage 中，也就是不能同其他Mapper一样被扫描到。        properties.setProperty("mappers", IBaseDao.class.getName());        properties.setProperty("ORDER", "BEFORE");        properties.setProperty("notEmpty", "false");        properties.setProperty("IDENTITY","SELECT REPLACE(UUID(),'-','')");        mapperScannerConfigurer.setProperties(properties);        return mapperScannerConfigurer;    }}