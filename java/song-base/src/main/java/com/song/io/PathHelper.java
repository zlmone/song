package com.song.io;import com.song.lang.StringHelper;import com.song.net.UrlHelper;import java.io.File;/** * description: * author:          song * createDate:      2017/10/19 */public class PathHelper {    //拼接符    public static final String SEPARATORREGEX = "\\\\";    //重复拼接符的正则    private static final String REPEATSEPARATORREGEX = "\\\\+";    /**     * 本地物理路径转为url路径     *     * @param path     * @return     */    public static String toUrlPath(String path) {        return path.replaceAll(PathHelper.SEPARATORREGEX, UrlHelper.SEPARATOR);    }    /**     * 拼接多个path散片     *     * @param paths     * @return     */    public static String combine(String... paths) {        return StringHelper.combine(PathHelper.SEPARATORREGEX, REPEATSEPARATORREGEX, paths);    }}