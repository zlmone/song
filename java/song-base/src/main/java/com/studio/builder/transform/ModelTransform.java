package com.studio.builder.transform;import com.song.db.DBType;import com.studio.builder.model.Column;import com.studio.builder.model.Table;import java.util.List;/** * description: * author:          song * createDate:      2017/10/25 */public abstract class ModelTransform {    protected DBType dbType;    public DBType getDbType() {        return dbType;    }    public void setDbType(DBType dbType) {        this.dbType = dbType;    }    /**     * 将数据源结构转换为生成器模型（原始字段转换）     * @return     */    protected abstract List<Table> getTables();    public List<Table> fillTables() {        List<Table> tables=this.getTables();        for (Table table : tables) {            List<Column> columns= table.getColumns();            for (Column column : columns) {            }        }        return tables;    }}